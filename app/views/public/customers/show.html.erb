<h4>マイページ</h4>

<div class="container">
  <div class="row">
  <label>登録情報 <%= link_to "編集する", customers_edit_path, class: "btn btn-success" %></label>
  </div>

  <div>
    <div>氏名 　　　　　　　<%= current_customer.last_name %><%= current_customer.first_name %></div>
    <div>カナ 　　　　　　　<%= current_customer.last_name_kana %><%= current_customer.first_name_kana %></div>
    <div>郵便番号 　　　　　<%= current_customer.postal_code %></div>
    <div>住所 　　　　　　　<%= current_customer.address %></div>
    <div>電話番号 　　　　　<%= current_customer.telephone_number %></div>
    <div>メールアドレス 　　<%= current_customer.email %></div>
  </div>

  <div class="row">
  <label>配送先 <%= link_to "一覧を見る", public_addresses_path, class: "btn btn-primary" %></label>
  </div>

  <div class="row">
  <label>注文履歴 <%= link_to "一覧を見る", public_orders_path, class: "btn btn-primary" %></label>
  </div>

</div>